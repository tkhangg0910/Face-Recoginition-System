# from authService import insert_user, search_user, show_data
# from db_config import get_collection

# db = get_collection()
# show_data(db)
from pathlib import Path
BASE_DIR = Path(__file__).resolve().parent.parent
print(BASE_DIR)